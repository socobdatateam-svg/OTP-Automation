services:
  seatalk-bot:
    build: .
    container_name: seatalk-bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./.env:/app/.env:ro
      - ./google-service-account.json:/app/google-service-account.json:ro
